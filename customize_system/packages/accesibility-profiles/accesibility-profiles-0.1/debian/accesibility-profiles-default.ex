# Defaults for accesibility-profiles initscript
# sourced by /etc/init.d/accesibility-profiles
# installed at /etc/default/accesibility-profiles by the maintainer scripts

#
# This is a POSIX shell fragment
#

# Additional options that are passed to the Daemon.
DAEMON_OPTS=""
